# Shell-compatible defaults read by the external tavi-dev/update.sh helper.
# Caller-provided environment values take precedence over these development defaults.
K8S_NAMESPACE="${K8S_NAMESPACE:-tavi-dev}"
K8S_CLUSTER="${K8S_CLUSTER:-sv4d-cops-nonp}"
