# Goal: Remediate and merge PR 55

## User Request

Work on #55 now. The goal is to get it to a merge-ready state, then merge it
into `main`.

## Refined Goal

Remediate Dependabot PR #55 (`npm-minor-patch` group with nine updates) so its
workspace validation and API image scan pass without weakening security policy
or bypassing branch protection. Preserve the intended dependency updates and
make only the targeted compatibility or security changes needed to resolve the
failures. Push the repair to the existing PR source branch, confirm the
required checks succeed and the PR is mergeable, then merge it into `main`.

## Acceptance Criteria

- [ ] PR #55 keeps the intended nine Dependabot minor/patch updates, with any
      additional manifest or lockfile change directly necessary to remediate a
      reproducible validation or scan failure.
- [ ] The dependency lockfile is generated by pnpm, all local validation
      commands applicable to the changed workspaces succeed, and no existing
      vulnerability exception is added or broadened.
- [ ] GitHub reports success for `Validate workspace`, `Build api image`,
      `Build web image`, `Build worker image`, `Scan api image`, `Scan web
      image`, and `Scan worker image` on the final PR head.
- [ ] PR #55 is clean and mergeable without force-pushing, bypassing branch
      protection, replacing the PR, or weakening workflow enforcement; it is
      then merged into `main`, and the merge commit is confirmed on `main`.

## Scope Boundaries

**In scope:**
- Diagnose and remediate the failed `Validate workspace` and `Scan api image`
  checks on PR #55.
- Update directly responsible dependency manifests, pnpm overrides, and the
  pnpm-generated lockfile when needed.
- Use the existing PR, its original Dependabot history, normal GitHub checks,
  review process, and merge policy.

**Out of scope:**
- Disabling, reordering, or weakening workspace validation, Trivy reporting,
  HIGH/CRITICAL enforcement, branch protection, or Dependabot policy.
- Adding or extending `.trivyignore.yaml` exceptions.
- Unrelated feature work, dependency major-version upgrades, or replacement
  pull requests.

## Applicable Project Conventions

**Quality gate command:**
- `corepack pnpm install --frozen-lockfile`
- `corepack pnpm --filter @tavi/config build`
- `corepack pnpm --filter @tavi/schemas build`
- `corepack pnpm --filter @tavi/api prisma:generate`
- `corepack pnpm lint`
- `corepack pnpm typecheck`
- `corepack pnpm test`
- Confirm the seven required GitHub checks listed in the acceptance criteria
  on the final PR head.

**Commit convention:**
- Conventional commits with the required role marker:
  `type(scope): [B] description` for Builder and
  `chore(scope): [I] description` for Inspector.
- Builder and Inspector each make one commit per iteration. Titles are at most
  72 characters.
- Builder commits include `Assisted-by: Claude:Sonnet-4.6`; Inspector commits
  include `Assisted-by: Claude:Haiku-4.5`.
- All commits also include
  `Co-authored-by: Copilot App <223556219+Copilot@users.noreply.github.com>`.

**Guidelines:**
- `docs/CI.md`: required validation, build, and scan workflow behavior.
- `docs/GITHUB-SETUP.md`: required branch-protection checks and merge policy.
- `docs/LCM.md`: dependency and lifecycle-management policy.
- `docs/DEVGUIDE.md`: local development and validation commands.
- `.github/dependabot.yml`: weekly grouped minor/patch update policy.
- `.github/workflows/publish-images.yml`: validation and image-build workflow.
- `.github/workflows/trivy-scan.yml`: image-scan reporting and enforcement.
- `.github/workflows/automerge-dependencies.yml`: Dependabot auto-merge rules.

**Rules:**
- Use Node `>=26 <27` and pnpm `10.33.0` through Corepack.
- Keep `pnpm-lock.yaml` pnpm-generated; do not manually format it.
- Do not use permanent or unowned Trivy suppressions; existing exceptions are
  time-bounded and must not be broadened.
- Required checks are `Validate workspace`, all three image builds, and all
  three image scans. Branches must be current before merging.
